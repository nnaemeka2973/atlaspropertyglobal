// /* Frontend contact configuration. Never put your Resend API key here. */
// window.ATLAS_CONTACT_CONFIG = {
//   // Optional fallback address used only when a supplied listing has no advertiser email.
//   fallbackEmail: "",
//   // The form submits to /api/contact, which uses RESEND_API_KEY on the server.
//   contactApi: "/api/contact"
// };
